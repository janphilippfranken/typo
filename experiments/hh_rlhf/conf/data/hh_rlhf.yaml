hydra:
  run:
    dir: outputs

dataset:
  path: Anthropic/hh-rlhf
  data_dir: harmless-base
  cache_dir: /scr/jphilipp/scai/datasets/hh-rlhf
  # dataset = load_from_disk(f"{args.data.dataset.cache_dir}_shuffled_{n_shuffle}") # load the shuffled dataset  if wanted to load a shuffled version of the dataset
  