hydra:
  run:
    dir: outputs

defaults:
  - model: mistral_7b_sft_cai_100_examples
  - data: hh_rlhf_cai

wandb:
  project: scai
  log_model: checkpoint
  name: dpo_cai_100_examples

validation_split_size: 0.05

dpo_beta: 0.05
max_prompt_length: 612

training_args:
  output_dir: /scr/jphilipp/scai/trained_models/Mistral-7B-v0.1/checkpoints/dpo/cai_100_examples
  per_device_train_batch_size: 2
  per_device_eval_batch_size: 2
  gradient_accumulation_steps: 64
  learning_rate: 5.0e-7
  num_train_epochs: 1
  save_total_limit: 1
  evaluation_strategy: epoch
  seed: 42
  save_strategy: epoch
  report_to: wandb
  bf16: true
  lr_scheduler_type: cosine
  warmup_ratio: 0.1
  do_eval: true
  logging_steps: 1
  logging_strategy: steps