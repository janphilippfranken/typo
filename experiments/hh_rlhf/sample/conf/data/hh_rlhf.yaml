hydra:
  run:
    dir: outputs

dataset:
  path: Anthropic/hh-rlhf
  data_dir: harmless-base
  cache_dir: /scr/jphilipp/scai/datasets/hh-rlhf

split: test

start_of_test_example: 1000 # will never see more than the first 1000 as the latter ones are used for eval